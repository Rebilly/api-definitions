# Do not allow installation of dependencies that were published less than 7 days ago.
# Matches minimumReleaseAge in pnpm-workspace.yaml (10080 minutes).
# Dependabot reads this as a cooldown floor for npm version updates.
min-release-age=7
