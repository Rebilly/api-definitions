content:
  application/json:
    schema:
      allOf:
        - $ref: ../schemas/Organization.yaml
        - properties:
            website:
              readOnly: true
description: Create Organization Request.
