type: object
required:
  - currentPassword
  - newPassword
properties:
  currentPassword:
    description: User's current password. This value is used to request a password change.
    type: string
    format: password
  newPassword:
    description: User's new password. This value replaces the existing password.
    type: string
    format: password
