description: JetPay gateway configuration.
allOf:
  - $ref: ./GatewayAccount.yaml
  - type: object
    required:
      - credentials
    properties:
      credentials:
        type: object
        properties:
          TerminalID:
            type: string
            description: JetPay terminal ID.
        required:
          - TerminalID
