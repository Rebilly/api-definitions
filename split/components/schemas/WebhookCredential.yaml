allOf:
  - $ref: ./Credential.yaml
  - type: object
    description: Webhook credential.
    properties:
      host:
        type: string
        description: Name of the host.
      auth:
        $ref: ./WebhookAuthorization.yaml
    required:
      - host
