description: PaymentsOS 3DS servers.
discriminator:
  propertyName: name
  mapping:
    ThreeDSecureIO3dsServer: ./PaymentsOSThreeDSecureIOServer.yaml
type: object
required:
  - name
properties:
  name:
    allOf:
      - $ref: ./ThreeDSecureServerName.yaml
    enum:
      - ThreeDSecureIO3dsServer
