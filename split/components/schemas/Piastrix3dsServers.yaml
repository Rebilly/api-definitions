description: Piastrix 3DS servers.
discriminator:
  propertyName: name
  mapping:
    Piastrix3dsServer: ./Piastrix3dsServer.yaml
type: object
required:
  - name
properties:
  name:
    allOf:
      - $ref: ./ThreeDSecureServerName.yaml
    enum:
      - Piastrix3dsServer
