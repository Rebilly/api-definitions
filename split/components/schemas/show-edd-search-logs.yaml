description: Show Enhanced Due Diligence (EDD) search logs action.
allOf:
  - $ref: ./TimelineAction.yaml
  - type: object
    properties:
      searchLogId:
        allOf:
          - $ref: ./ResourceId.yaml
      type:
        type: string
        enum:
          - arrest
          - bankruptcy
          - fraud
          - occupation
