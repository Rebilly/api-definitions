post:
  summary: Gateway account limit reached
  operationId: gateway-account-limit-reached
  x-products:
    - Users
  tags:
    - Gateway accounts
  requestBody:
    $ref: ../components/requestBodies/GatewayAccountAndGatewayAccountLimit.yaml
  responses:
    2XX:
      description: Returns any 2xx status to indicate that the data received.
