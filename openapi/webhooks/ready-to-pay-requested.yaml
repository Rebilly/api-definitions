summary: Ready to pay requested
operationId: ready-to-pay-requested
x-products:
  - Core
tags:
  - Transactions
requestBody:
  $ref: ../components/requestBodies/Webhooks/ReadyToPayRequested.yaml
responses:
  '2xx':
    description: Returns any 2xx status to indicate that data is received.
