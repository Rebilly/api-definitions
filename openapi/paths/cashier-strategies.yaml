post:
  x-products:
    - Core
  tags:
    - Cashier
  summary: Create a cashier strategy
  operationId: PostCashierStrategy
  x-sdk-operation-name: create
  description: |
    Create a cashier strategy.
    Then, redirect the customer to the `cashier` link.
    After the customer completes the deposit, they will be redirected back to the `redirectUrl`.
    Corresponding transaction webhooks are sent to webhooks subscribers.
  requestBody:
    content:
      application/json:
        schema:
          $ref: ../components/schemas/Cashier/CashierStrategy.yaml
    description: Cashier strategy resource.
    required: true
  responses:
    '201':
      description: Cashier strategy was created.
      headers:
        Location:
          $ref: ../components/headers/Location.yaml
      content:
        application/json:
          schema:
            $ref: ../components/schemas/Cashier/CashierStrategy.yaml
    '401':
      $ref: ../components/responses/Unauthorized.yaml
    '403':
      $ref: ../components/responses/Forbidden.yaml
    '422':
      $ref: ../components/responses/ValidationError.yaml
get:
  x-products:
    - Core
  tags:
    - Cashier
  summary: Retrieve cashier strategies
  operationId: GetCashierStrategyCollection
  x-sdk-operation-name: getAll
  description: |
    Retrieve a list of cashier strategirs.
  parameters:
    - $ref: ../components/parameters/collectionLimit.yaml
    - $ref: ../components/parameters/collectionOffset.yaml
    - $ref: ../components/parameters/collectionFilter.yaml
    - $ref: ../components/parameters/collectionSort.yaml
  responses:
    '200':
      description: A list of cashier strategies was retrieved.
      headers:
        Pagination-Total:
          $ref: ../components/headers/Pagination-Total.yaml
        Pagination-Limit:
          $ref: ../components/headers/Pagination-Limit.yaml
        Pagination-Offset:
          $ref: ../components/headers/Pagination-Offset.yaml
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: ../components/schemas/Cashier/CashierStrategy.yaml
    '401':
      $ref: ../components/responses/Unauthorized.yaml
    '403':
      $ref: ../components/responses/Forbidden.yaml
