parameters:
  - $ref: ../../components/parameters/resourceId.yaml
post:
  x-badge: Experimental
  x-products:
    - Storefront
  tags:
    - Storefront orders
  summary: Change subscription order items
  operationId: StorefrontPostOrderItemsChange
  x-sdk-operation-name: changeItems
  security:
    - CustomerJWT: []
  description: Creates a quote for change items and quantities.
  requestBody:
    content:
      application/json:
        schema:
          $ref: ../../components/requestBodies/storefront/PostOrderItemsChange.yaml
    description: Change items request.
    required: true
  responses:
    '200':
      description: Quote for change items created and issued.
      headers:
        Location:
          $ref: ../../components/headers/Location.yaml
      content:
        application/json:
          schema:
            $ref: ../../components/schemas/StorefrontQuoteChangeOrder.yaml
    '401':
      $ref: ../../components/responses/Unauthorized.yaml
    '403':
      $ref: ../../components/responses/Forbidden.yaml
    '404':
      $ref: ../../components/responses/NotFound.yaml
    '422':
      $ref: ../../components/responses/ValidationError.yaml
