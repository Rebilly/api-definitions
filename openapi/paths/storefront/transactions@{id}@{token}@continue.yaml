parameters:
  - $ref: ../../components/parameters/resourceId.yaml
  - $ref: ../../components/parameters/token.yaml
post:
  x-products:
    - Storefront
  tags:
    - Transactions
  summary: Complete KYC request
  operationId: StorefrontPostKycRequestContinue
  x-sdk-operation-name: completeKycRequest
  security:
    - CustomerJWT: []
  description: |-
    Completes KYC request with a specified token, and continue processing transaction with a specified ID.

    Use this operation to complete a KYC request in a transaction flow, and continue processing the transaction.
  responses:
    200:
      description: KYC request completed.
      content:
        application/json:
          schema:
            $ref: ../../components/schemas/Storefront/StorefrontTransaction.yaml
    401:
      $ref: ../../components/responses/Unauthorized.yaml
    403:
      $ref: ../../components/responses/Forbidden.yaml
    404:
      $ref: ../../components/responses/NotFound.yaml
