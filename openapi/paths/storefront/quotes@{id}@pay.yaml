parameters:
  - $ref: ../../components/parameters/resourceId.yaml
post:
  x-products:
    - Storefront
  tags:
    - Quotes
  summary: Pay a quote
  operationId: StorefrontPostQuotePayment
  x-sdk-operation-name: pay
  security:
    - CustomerJWT: []
  description: |-
    Creates a quote transaction.
    Once the transaction is approved the quote is automatically accepted.
  responses:
    201:
      description: Transaction created.
      headers:
        Location:
          $ref: ../../components/headers/Location.yaml
      content:
        application/json:
          schema:
            $ref: ../../components/schemas/StorefrontTransaction.yaml
    401:
      $ref: ../../components/responses/Unauthorized.yaml
    403:
      $ref: ../../components/responses/Forbidden.yaml
    404:
      $ref: ../../components/responses/NotFound.yaml
    422:
      $ref: ../../components/responses/ValidationError.yaml
