parameters:
  - $ref: ../../components/parameters/resourceId.yaml
  - $ref: ../../components/parameters/token.yaml
post:
  x-products:
    - Storefront
  tags:
    - Transactions
  summary: Bypass KYC request
  operationId: StorefrontPostKycRequestBypass
  x-sdk-operation-name: bypassKycRequest
  security:
    - CustomerJWT: []
  description: Bypasses optional KYC request with a specified token, for transaction with a specified ID.
  responses:
    200:
      description: Transaction processing completed.
      content:
        application/json:
          schema:
            $ref: ../../components/schemas/Storefront/StorefrontTransaction.yaml
    401:
      $ref: ../../components/responses/Unauthorized.yaml
    403:
      $ref: ../../components/responses/Forbidden.yaml
    404:
      $ref: ../../components/responses/NotFound.yaml
