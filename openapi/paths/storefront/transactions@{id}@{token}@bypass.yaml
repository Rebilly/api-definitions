parameters:
  - $ref: ../../components/parameters/resourceId.yaml
  - $ref: ../../components/parameters/token.yaml
post:
  x-products:
    - Storefront
  tags:
    - Transactions
  summary: Skip KYC request
  operationId: StorefrontPostKycRequestBypass
  x-sdk-operation-name: bypassKyc
  security:
    - CustomerJWT: []
  description: |-
    Bypasses an optional KYC request with a specified token, for a transaction with a specified ID.

    Use this operation to bypass a KYC request that has been marked as optional in a transaction flow, so that you can further process the transaction.
  responses:
    200:
      description: KYC request bypassed.
      content:
        application/json:
          schema:
            $ref: ../../components/schemas/Storefront/StorefrontTransaction.yaml
    401:
      $ref: ../../components/responses/Unauthorized.yaml
    403:
      $ref: ../../components/responses/Forbidden.yaml
    404:
      $ref: ../../components/responses/NotFound.yaml
