get:
  x-products:
    - Core
  tags:
    - AML
  summary: Retrieve customer AML checks
  operationId: GetAmlCheckCollection
  x-sdk-operation-name: getAll
  description: |-
    Retrieves a list of customer AML checks.
    These checks are records of customer data and potentially matching data in AML lists.
  parameters:
    - $ref: ../components/parameters/collectionLimit.yaml
    - $ref: ../components/parameters/collectionOffset.yaml
    - $ref: ../components/parameters/collectionSort.yaml
  responses:
    '200':
      description: A list of AML checks was retrieved.
      headers:
        Pagination-Total:
          $ref: ../components/headers/Pagination-Total.yaml
        Pagination-Limit:
          $ref: ../components/headers/Pagination-Limit.yaml
        Pagination-Offset:
          $ref: ../components/headers/Pagination-Offset.yaml
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: ../components/schemas/AmlCheck.yaml
