Paths
=====

- Paths are plural.
- Write each path specification in separate file
- Filename is mapped to path by replacing `@` with `/`, for example, `user@{username}.yaml` matches to `user/{username}` path
