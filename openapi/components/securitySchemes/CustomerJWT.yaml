description: >
  You can create a JSON Web Token (JWT) via [Storefront
  Authentication](https://storefront-api-docs.rebilly.com/#operation/StorefrontPostLogin).

  Usage format: `Bearer <JWT>`.
type: http
scheme: bearer
bearerFormat: JWT
