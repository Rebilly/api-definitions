content:
  application/json:
    schema:
      properties:
        recognitionId:
          description: Revenue recognition ID of the invoice.
          type: string
        invoiceId:
          description: Invoice ID.
          type: string
        itemId:
          description: Invoice item ID.
          type: string
        amount:
          description: Amount recognized.
          type: number
          format: double
        currency:
          $ref: ../../schemas/CurrencyCode.yaml
        eventType:
          type: string
          enum:
            - invoice-revenue-recognized
        _embedded:
          type: object
          description: |-
            Available embedded objects that are requested by the
            expand querystring parameter.
          properties:
            invoice:
              $ref: ../../schemas/Invoices/Invoice.yaml
        _links:
          type: array
          description: Related resource links.
          minItems: 1
          items:
            anyOf:
              - $ref: ../../schemas/Links/InvoiceLink.yaml
description: Invoice webhook request body resource.
