content:
  application/json:
    schema:
      properties:
        organizationId:
          $ref: ../../schemas/OrganizationId.yaml
        taxIdNumber:
          type: string
          description: Value of the tax number.
          example: GB980780684
        taxIdType:
          type: string
          description: Type of the tax ID number.
          enum:
            - eu-vat
            - other
        isValid:
          type: boolean
          description: Determines if the tax number passed the EU official [VIES validation](https://ec.europa.eu/taxation_customs/vies/#/vat-validation).
        eventType:
          description: Rebilly webhooks event type.
          type: string
          enum:
            - organization-tax-number-validated
        _embedded:
          type: object
          description: Embedded objects.
          properties:
            organization:
              $ref: ../../schemas/Organization.yaml
        _links:
          type: array
          description: Related links.
          items:
            type: object
            properties:
              href:
                description: Link URL.
                type: string
              rel:
                description: Type of link.
                type: string
                enum:
                  - organization
description: Organization tax number validated webhook request body resource.
