content:
  application/json:
    schema:
      properties:
        paymentCardId:
          description: The payment card ID
          type: string
          readOnly: true
        eventType:
          $ref: ../../schemas/GlobalWebhookEventType.yaml
        _embedded:
          $ref: ../../schemas/Embeds/PaymentCardEmbed.yaml
        _links:
          type: array
          description: The links related to resource
          readOnly: true
          minItems: 1
          items:
            anyOf:
              - $ref: ../../schemas/Links/PaymentCardLink.yaml
description: Payment card webhook request body resource