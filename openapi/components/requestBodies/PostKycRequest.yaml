content:
  application/json:
    schema:
      allOf:
        - required:
            - customerId
          properties:
            customerId:
              description: The сustomer's ID.
              allOf:
                - $ref: ../schemas/ResourceId.yaml
        - $ref: ../schemas/KycRequest.yaml
