content:
  application/json:
    schema:
      allOf:
        - $ref: ../schemas/Organization.yaml
        - required:
            - name
            - website
            - country
            - reportCurrency
description: Create organization request.
