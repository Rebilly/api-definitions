content:
  application/json:
    schema:
      type: object
      required:
        - quantityUsed
      properties:
        quantityUsed:
          description: Used quantity of the subscription item.
          type: integer
description: Order item update resource.
required: true
