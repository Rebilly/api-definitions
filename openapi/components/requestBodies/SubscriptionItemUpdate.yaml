type: object
required:
  - quantityFilled
properties:
  quantityFilled:
    x-badges:
      - name: Experimental
    description: Filled quantity of the subscription item (experimental property).
    type: integer
