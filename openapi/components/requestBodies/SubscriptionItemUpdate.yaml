content:
  application/json:
    schema:
      type: object
      required:
        - quantityUsed
      properties:
        quantityUsed:
          x-badge: Experimental
          description: Used quantity of the subscription item (experimental property).
          type: integer
description: Order item update resource.
required: true
