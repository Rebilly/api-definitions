content:
  application/json:
    schema:
      properties:
        organizationId:
          type: string
          description: Unique resource ID.
          maxLength: 50
          example: org_0YVDM8RC7GDADADSBSMW124JA8
        userId:
          $ref: ../schemas/ResourceId.yaml
        isOwner:
          type: boolean
        permissions:
          description: Memberships's permissions. See the format in example. Use wildcard `*` for full access.
          allOf:
            - $ref: ../schemas/AclPermissions.yaml
description: Membership resource.
required: true
