content:
  application/json:
    schema:
      allOf:
        - $ref: ../schemas/Dispute.yaml
        - required:
            - reasonCode
      properties:
        reasonCode:
          type: string
description: Create dispute.
