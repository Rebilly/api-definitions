content:
  application/json:
    schema:
      allOf:
        - $ref: ../schemas/Dispute.yaml
        - required:
            - reasonCode
        - reasonCode:
            type: string
description: Create dispute.
