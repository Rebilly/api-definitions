content:
  application/json:
    schema:
      anyOf:
        - $ref: ../schemas/SmtpCredential.yaml
        - $ref: ../schemas/WebhookCredential.yaml
        - $ref: ../schemas/ExperianCredential.yaml
        - $ref: ../schemas/MailgunCredential.yaml
        - $ref: ../schemas/OAuth2Credential.yaml
        - $ref: ../schemas/PostmarkCredential.yaml
        - $ref: ../schemas/SendGridCredential.yaml
        - $ref: ../schemas/SESCredential.yaml
        - $ref: ../schemas/TaxJarCredential.yaml
        - $ref: ../schemas/AvalaraCredential.yaml
        - $ref: ../schemas/PlaidCredential.yaml
description: Service credential resource.
required: true
