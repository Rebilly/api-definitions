content:
  application/json:
    schema:
      type: object
      required:
        - cashierRequestId
        - amount
      properties:
        cashierRequestId:
          $ref: ../../schemas/ResourceId.yaml
        amount:
          description: Deposit amount.
          type: number
          format: double
          example: 97.97
required: true
