content:
  application/json:
    schema:
      type: object
      required:
        - token
      properties:
        token:
          description: ID of the payment instrument token.
          type: string
        customFields:
          $ref: ../../schemas/ResourceCustomFields.yaml
description: Payment instrument.
required: true
