content:
  application/json:
    schema:
      allOf:
        - $ref: ../schemas/Organization.yaml
        - properties:
            website:
              readOnly: true
            reportCurrency:
              readOnly: true
description: Update Organization Request.
