content:
  application/json:
    schema:
      allOf: 
        - $ref: ../schemas/Organizations/Organization.yaml
        - properties:
            website:
              readOnly: true
description: Create Organization Request.
