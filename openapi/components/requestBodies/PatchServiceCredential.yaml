content:
  application/json:
    schema:
      anyOf:
        - $ref: ../schemas/Credentials/PatchCredential.yaml
        - $ref: ../schemas/Credentials/PatchPlaidCredential.yaml
description: Service credential resource.
required: true
