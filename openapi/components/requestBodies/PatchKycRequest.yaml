content:
  application/json:
    schema:
      allOf:
        - properties:
            customerId:
              readOnly: true
        - $ref: ../schemas/KycRequest.yaml
description: KYC request resource.
required: true
