content:
  application/json:
    schema:
      $ref: ../schemas/SubscriptionCancellation.yaml
      type: object
      properties:
        churnTime:
          description: Date and time when the subscription is deactivated.
          type: string
          enum:
            - now
            - at-next-renewal
description: Cancellation resource.
required: true
