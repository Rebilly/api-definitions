content:
  application/json:
    schema:
      anyOf:
            - $ref: ../schemas/OneTimeSalePlan.yaml
            - $ref: ../schemas/SubscriptionOrderPlan.yaml
            - $ref: ../schemas/TrialOnlyPlan.yaml
description: Plan resource.
required: true
