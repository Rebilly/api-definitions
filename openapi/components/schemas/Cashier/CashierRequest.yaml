type: object
required:
  - websiteId
  - customerId
  - currency
properties:
  id:
    readOnly: true
    allOf:
      - $ref: ../ResourceId.yaml
  websiteId:
    description: Website ID of the cashier. This value specifies the website with which the cashier is associated.
    type: string
    maxLength: 50
    example: 4f6cf35x-2c4y-483z-a0a9-158621f77a21
  customerId:
    $ref: ../CustomerId.yaml
  strategyId:
    description: |-
      Strategy ID of a strategy to be applied for this request for empty `amounts` and `customAmount`.
      If it is not specified, randomly selected strategy with a matching `filter` value is applied.
    type: string
    maxLength: 50
    example: 4f6cf35x-2c4y-483z-a0a9-158621f77a21
    writeOnly: true
  transactionId:
    type: string
    description: ID of the transaction that is used in the cashier request.
    readOnly: true
    nullable: true
    maxLength: 50
    example: 4f6cf35x-2c4y-483z-a0a9-158621f77a21
  status:
    description: Status of the request.
    type: string
    readOnly: true
    enum:
      - created
      - pending
      - completed
      - expired
    x-enumDescriptions:
      created: Request is created, but it has not been visited by a customer. This is a temporary state.
      pending: Request has been visited by a customer, but no funds have been deposited yet. This is a temporary state.
      completed: A funds deposit transaction has been initiated. This is a permanent state.
      expired: Request expired without a deposit attempt. This is a permanent state.
  currency:
    $ref: ../CurrencyCode.yaml
  amounts:
    type: array
    description: |-
      List of available deposit amounts.

      If `amounts` is not specified when a cashier is created, amounts are determined from a chosen strategy.
      For more information, see the [`strategyId` property](../operation/PostCashierRequest#!path=strategyId&t=request).
    items:
      type: number
      format: double
      minimum: 0.01
  customAmount:
    $ref: ./CashierStrategyCustomAmount.yaml
  redirectUrl:
    description: URL to redirect the customer to when a deposit is completed. The default value is the website URL.
    type: string
    format: uri
  expirationTime:
    description: Date and time at which the cashier request expires. The default expiration time is one hour from the time the request is created.
    type: string
    format: date-time
  createdTime:
    $ref: ../CreatedTime.yaml
  updatedTime:
    $ref: ../UpdatedTime.yaml
  _links:
    type: array
    description: Related links.
    readOnly: true
    minItems: 1
    items:
      anyOf:
        - $ref: ../Links/SelfLink.yaml
        - $ref: ../Links/CashierLink.yaml
        - $ref: ../Links/TransactionLink.yaml
  _embedded:
    type: array
    description: Embedded objects that are requested by the `expand` query parameter.
    readOnly: true
    minItems: 0
    items:
      anyOf:
        - $ref: ../Embeds/CustomerEmbed.yaml
        - $ref: ../Embeds/WebsiteEmbed.yaml
        - $ref: ../Embeds/TransactionEmbed.yaml
