type: object
description: The custom amount allowance selection strategy.
required:
  - name
discriminator:
  propertyName: name
  mapping:
    static: ./CashierCustomAmountAllowanceStrategyStatic.yaml
    condition: ./CashierCustomAmountAllowanceStrategyCondition.yaml
properties:
  name:
    type: string
    description: The name of the strategy.
    enum:
      - static
      - condition
    x-enumDescriptions:
      static: Custom amount flag is set by the `value` field.
      condition: Use `filter` to define distinctive cases and apply different strategies based on it.
