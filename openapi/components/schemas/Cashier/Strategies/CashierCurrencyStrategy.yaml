type: object
description: The currency selection strategy.
required:
  - name
discriminator:
  propertyName: name
  mapping:
    static: ./CashierCurrencyStrategyValued.yaml
    country: ./CashierCurrencyStrategyValued.yaml
    language: ./CashierCurrencyStrategyValued.yaml
    condition: ./CashierCurrencyStrategyCondition.yaml
properties:
  name:
    type: string
    description: The name of the strategy.
    enum:
      - currency
      - country
      - language
      - condition
    x-enumDescriptions:
      currency: Currency code is set by the `value` field.
      country: Use primary country currency, defined by the `value` field.
      language: Use currency, associated with the language set by `value` field.
      condition: Use `filter` to define distinctive cases and apply different strategies based on it.
