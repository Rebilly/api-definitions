type: object
allOf:
  - $ref: ./CashierCurrencyStrategy.yaml
  - required:
      - value
    properties:
      value:
        type: string
        description: |-
          The value corresponding to the strategy name.

          Use mustache templates with `cashierRequest` variable.
