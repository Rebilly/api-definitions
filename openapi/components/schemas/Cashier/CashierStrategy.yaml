type: object
required:
  - name
  - filter
  - amounts
  - customAmount
properties:
  id:
    readOnly: true
    allOf:
      - $ref: ../ResourceId.yaml
  name:
    type: string
    description: The strategy name.
  filter:
    type: string
    description: >-
      A filter based on a cashier request properties which is used to determine if the strategy is applicable.
      Empty filter matches any case.
    default: ''
    example: 'cashierRequest.currency:USD'
    allOf:
      - $ref: ../FilterString.yaml
  amounts:
    $ref: ./CashierStrategyAmounts.yaml
  customAmount:
    description: |-
      Custom amount restrictions.
      
      If this value is `null,  custom amounts are prohibited.
    nullable: true
    allOf:
      - $ref: ./CashierStrategyCustomAmount.yaml
  createdTime:
    description: Date and time at which the cashier strategy was created.
    allOf:
      - $ref: ../ServerTimestamp.yaml
  updatedTime:
    $ref: ../UpdatedTime.yaml
