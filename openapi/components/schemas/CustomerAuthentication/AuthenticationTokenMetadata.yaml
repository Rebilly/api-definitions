type: object
properties:
  expiredTime:
    description: Date and time at which the token expired.
    type: string
    format: date-time
  _links:
    type: array
    description: Related resource links.
    readOnly: true
    minItems: 1
    items:
      $ref: ../Links/SelfLink.yaml
