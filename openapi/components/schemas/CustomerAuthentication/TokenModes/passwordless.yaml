required:
  - customerId
allOf:
  - $ref: ../AuthenticationToken.yaml
  - properties:
      customerId:
        description: The token's customer ID
        allOf:
          - $ref: ../../ResourceId.yaml
  - $ref: ../AuthenticationTokenMetadata.yaml
