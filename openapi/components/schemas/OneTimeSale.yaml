allOf:
  - $ref: ./BaseOneTimeSale.yaml
  - type: object
    required:
      - type
      - items
    properties:
      type:
        description: Specifies the type of order.
        type: string
        x-basic: true
        enum:
          - one-time-sale
