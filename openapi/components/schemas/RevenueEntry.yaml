type: object
readOnly: true
properties:
  customerId:
    description: Identifier of the customer which generated the revenue.
    allOf:
      - $ref: ./ResourceId.yaml
    type: string
  invoiceId:
    description: Identifier of the invoice with the item which generated the revenue.
    allOf:
      - $ref: ./ResourceId.yaml
    type: string
  invoiceItemId:
    description: Identifier of the invoice item which generated the revenue.
    allOf:
      - $ref: ./ResourceId.yaml
    type: string
  status:
    description: The entry's recognition status.
    readOnly: true
    type: string
    enum:
      - scheduled
      - recognized
    x-enumDescriptions:
      scheduled: The amount will be recognized at the end of month.
      recognized: The amount is recognized and final.
  estimatedAmount:
    description: Estimated amount which will be recognized unless it is changed before recognition time.
    type: number
    x-type: Money
    format: double
  recognizedAmount:
    description: The recognized amount.
    type: number
    x-type: Money
    format: double
  scheduledTime:
    description: Date and time at which the entry was created.
    allOf:
      - $ref: ./ServerTimestamp.yaml
  recognizedTime:
    description: Date and time at which the entry was recognized.
    nullable: true
    allOf:
      - $ref: ./ServerTimestamp.yaml
