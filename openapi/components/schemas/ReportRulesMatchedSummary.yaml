type: object
properties:
  data:
    type: array
    items:
      type: object
      properties:
        rule:
          description: |
            Rule matched name.
          type: string
        count:
          description: Count of matched rule.
          type: integer
        approvalRate:
          description: Transaction approval rate by rule.
          type: number
          format: double
