allOf:
  - $ref: ../PlanPriceFormula.yaml
  - type: object
    required:
      - price
    properties:
      price:
        description: >
          For the very simple price when it's fixed and does not depend on the
          quantity chosen by customer.


          If the price is 0, it's free.
        type: number
        format: double
        example: 99.95
