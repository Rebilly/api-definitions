type: object
required:
  - unit
  - length
properties:
  unit:
    description: The unit of time.
    type: string
    enum:
      - day
      - week
      - month
      - year
  length:
    description: The length of time.
    type: integer
