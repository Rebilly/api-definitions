allOf:
  - $ref: ./RuleAction.yaml
  - type: object
    properties:
      skipStartedServicePeriod:
        type: boolean
        description: Skip invoices with started service period.
        default: false
