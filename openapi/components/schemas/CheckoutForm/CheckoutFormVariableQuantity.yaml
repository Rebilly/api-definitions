allOf:
  - $ref: CheckoutFormQuantity.yaml
  - type: object
    required:
      - default
    properties:
      default:
        description: The default amount of the item
        type: integer
      minimum:
        description: The minimum amount of the item
        type: integer
        minimum: 1
        default: 1
      multipleOf:
        description: The step amount of the item
        type: integer
        minimum: 1
        default: 1
      maximum:
        description: The maximum amount of the item
        type: integer
        minimum: 1
        default: 100
