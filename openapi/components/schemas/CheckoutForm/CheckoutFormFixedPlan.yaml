title: Fixed quantity
type: object
required:
  - planId
  - quantity
properties:
  planId:
    description: The plan identifier string.
    allOf:
      - $ref: ../ResourceId.yaml
  quantity:
    description: The quantity.
    type: integer
    minimum: 1
