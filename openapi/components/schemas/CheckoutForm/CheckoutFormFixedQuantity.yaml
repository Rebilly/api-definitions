allOf:
  - $ref: CheckoutFormQuantity.yaml
  - type: object
    required:
      - amount
    properties:
      amount:
        description: The amount of the item
        type: integer
        minimum: 1
