type: object
required:
  - planId
  - quantity
properties:
  planId:
    description: The plan identifier string.
    allOf:
        - $ref: ../ResourceId.yaml
  quantity:
    description: The plan quantity options.
    allOf:
      - $ref: CheckoutFormQuantity.yaml
