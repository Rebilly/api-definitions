type: object
discriminator:
  propertyName: type
  mapping:
    fixed: CheckoutFormFixedQuantity.yaml
    variable: CheckoutFormVariableQuantity.yaml
required:
  - type
properties:
  type:
    description: If `variable` customers will be able to choose the quantity.
    type: string
    enum:
      - fixed
      - variable
