type: object
readOnly: true
properties:
  type:
    type: string
    enum:
      - document-unreadable
      - document-expired
      - document-not-matching
      - underage-person
      - other
  message:
    description: The rejection message.
    type: string
    example: Provided document is unreadable
