type: object
properties:
  firstName:
    description: Customer first name if it matches, null otherwise.
    type: string
    nullable: true
    example: John
  lastName:
    description: Customer last name if it matches, null otherwise.
    type: string
    nullable: true
    example: Doe
  documentSubtype:
    description: Interpreted subtype of the uploaded document.
    type: string
    nullable: true
    example: bank-statement
