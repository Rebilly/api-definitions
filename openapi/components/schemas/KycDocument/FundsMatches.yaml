type: object
properties:
  firstName:
    description: The customer first name if it was matched, null otherwise.
    type: string
    example: John
  lastName:
    description: The customer last name if it was matched, null otherwise.
    type: string
    example: Doe
  documentType:
    description: The interpreted type of the uploaded document.
    type: string
    example: bank-statement
