type: object
properties:
  creditBureau:
    description: The credit bureau which is used to get credit file data.
    type: string
    readOnly: true
    enum:
      - equifax
      - experian
      - transunion
      - test-bureau
  creditFileNumber:
    description: The credit file number on credit bureau.
    type: string
    readOnly: true
  name:
    description: Name of the person to consult the credit file.
    type: string
    readOnly: true
  consultedDate:
    description: Date to cosult with credit file bureau.
    type: string
    format: date-time
    readOnly: true
  decision:
    description: Value based on if the credit file can be used as a single source proof of identity decision, requires at least one more source (dual-source), or other for anything else.
    type: string
    enum:
      - single-source
      - dual-source
      - other
    readOnly: true
  dualSourceData:
    description: Contain 2 objects from different source like statements, letters, certificates, forms or other information sources.
    type: object
    readOnly: true
  referenceData:
    description: Extra data from credit bureau.
    type: object
    readOnly: true
