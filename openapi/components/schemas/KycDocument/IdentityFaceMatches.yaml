allOf:
  - $ref: ./IdentityMatches.yaml
  - properties:
      photoMatches:
        description: >-
          Checks if the face on the photo matches the face on the document.
        type: boolean
        readOnly: true
        example: true
