type: object
properties:
  weights:
    type: object
    description: Weights of all matched properties.
    properties:
      containsImage:
        type: integer
        minimum: 0
        maximum: 100
        description: Weight of containsImage.
      isIdentityDocument:
        type: integer
        minimum: 0
        maximum: 100
        description: Weight of isIdentityDocument.
      isPublishedOnline:
        type: integer
        minimum: 0
        maximum: 100
        description: Weight of isPublishedOnline.
      firstName:
        type: integer
        minimum: 0
        maximum: 100
        description: Weight of firstName.
      lastName:
        type: integer
        minimum: 0
        maximum: 100
        description: Weight of lastName.
      expirationDate:
        type: integer
        minimum: 0
        maximum: 100
        description: Weight of `expirationDate`.
      dateOfBirth:
        type: integer
        minimum: 0
        maximum: 100
        description: Weight of dateOfBirth.
      matchesDateOfBirth:
        type: integer
        minimum: 0
        maximum: 100
        description: Weight of matchesDateOfBirth.
      issueDate:
        type: integer
        minimum: 0
        maximum: 100
        description: Weight of issueDate.
      hasMinimalAge:
        type: integer
        minimum: 0
        maximum: 100
        description: Weight of hasMinimalAge.
      hasMatchingFaceProof:
        type: integer
        minimum: 0
        maximum: 100
        description: Weight of hasMatchingFaceProof.
      nationality:
        type: integer
        minimum: 0
        maximum: 100
        description: Weight of nationality.
      documentSubtype:
        type: integer
        minimum: 0
        maximum: 100
        description: Weight of documentSubtype.
  thresholds:
    type: object
    description: Thresholds to determine the document status.
    properties:
      rejectBelow:
        type: integer
        minimum: 0
        maximum: 100
        description: Weight of rejectBelow.
      acceptAbove:
        type: integer
        minimum: 0
        maximum: 100
        description: Weight of acceptAbove.
