type: object
properties:
  currency:
    description: Currency (three letter ISO 4217 alpha code). For example, USD, EUR.
    readOnly: true
    type: string
  refundsAmount:
    description: Sum of all refunded transactions.
    readOnly: true
    type: number
    format: double
  revenueAmount:
    description: Sum of overall sales and captures that were approved.
    readOnly: true
    type: number
    format: double
  disputesAmount:
    description: Sum of all disputed transactions.
    readOnly: true
    type: number
    format: double
