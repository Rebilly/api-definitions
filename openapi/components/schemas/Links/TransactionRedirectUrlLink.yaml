type: object
allOf:
  - $ref: ./Link.yaml
properties:
  rel:
    description: Redirect URL with transaction ID and transaction Result replaced.
    type: string
    enum:
      - redirectUrl
required:
  - rel
