type: object
allOf:
  - $ref: ./Link.yaml
properties:
  rel:
    description: >-
      The authentication URL where the merchant should be redirected to grant
      the requested access
    type: string
    enum:
      - redirect
required:
  - rel
