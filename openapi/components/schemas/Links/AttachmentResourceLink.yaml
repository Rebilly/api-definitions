type: object
allOf:
  - $ref: ./Link.yaml
properties:
  rel:
    description: Type of link.
    type: string
    enum:
      - customer
      - dispute
      - invoice
      - payment
      - plan
      - product
      - subscription
      - transaction
required:
  - rel
