type: object
allOf:
  - $ref: ./Link.yaml
properties:
  rel:
    description: The link type.
    type: string
    enum:
      - customer
      - dispute
      - invoice
      - payment
      - plan
      - product
      - subscription
      - transaction
required:
  - rel
