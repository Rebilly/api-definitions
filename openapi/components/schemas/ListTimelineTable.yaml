allOf:
  - $ref: ./TimelineTable.yaml
  - type: object
    properties:
      data:
        type: array
        description: |-
          Table data.
          Each array element represents a table row.
        items:
          type: string
