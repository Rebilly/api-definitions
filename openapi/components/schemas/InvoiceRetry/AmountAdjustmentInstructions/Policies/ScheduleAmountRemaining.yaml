title: Schedule autopay for amount remaining
type: object
properties:
  method:
    type: string
    enum:
      - schedule-amount-remaining
    description: Schedule the amount remaining for autopay.
  schedule:
    $ref: ../../../Scheduling/InvoiceRetryScheduleInstruction.yaml
