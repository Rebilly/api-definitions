type: object
required:
  - type
  - currency
  - amount
properties:
  type:
    type: string
    enum: ["fixed-fee"]
  currency:
    $ref: ../../CurrencyCode.yaml
  amount:
    type: number
    format: double
    example: 10.0
    x-type: Money

