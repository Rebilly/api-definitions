type: object
required:
  - type
  - currency
  - amount
properties:
  type:
    type: string
    enum: ["fixed-fee"]
  currency:
    $ref: ../../CurrencyCode.yaml
  amount:
    $ref: ../../MoneyAmount.yaml

