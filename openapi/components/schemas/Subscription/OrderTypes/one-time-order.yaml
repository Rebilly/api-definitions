allOf:
  - $ref: ../../Subscription.yaml
  - properties:
      status:
        description: One-time order status
        type: string
        readOnly: true
        enum:
          - pending
          - completed
          - abandoned
  - $ref: ../SubscriptionMetadata.yaml
