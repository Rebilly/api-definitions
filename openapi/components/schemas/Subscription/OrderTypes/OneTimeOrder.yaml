type: object
required:
  - orderType
  - customerId
  - websiteId
  - items
allOf:
  - $ref: ../../Subscription.yaml
  - $ref: ../../Common/CommonOneTimeOrder.yaml
  - properties:
      customerId:
        x-basic: true
        allOf:
          - $ref: ../../CustomerId.yaml
      organizationId:
        allOf:
          - $ref: ../../OrganizationId.yaml
  # Always keep the SubscriptionMetadata from the latest merged schema,
  # it contains _links and _embedded which is better to keep at the end of doc.
  - $ref: ../SubscriptionMetadata.yaml
