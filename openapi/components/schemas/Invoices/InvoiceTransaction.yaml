type: object
required:
  - transactionId
properties:
  transactionId:
    description: Transaction which needs to be assigned to invoice
    type: string
  amount:
    description: >
      Amount which needs to be assigned to the invoice. Can't be more than transaction's amount.
      If omitted, transaction's full amount will be used.
    type: number
    format: double
