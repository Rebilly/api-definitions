description: Rebilly-based shipping which is calculated by shipping zones.
allOf:
  - $ref: ../InvoiceShipping.yaml
  - type: object
    properties:
      amount:
        type: integer
        readOnly: true
        description: The invoice's shipping amount which is calculated from [Shipping Zones](#tag/Shipping-Zones).
