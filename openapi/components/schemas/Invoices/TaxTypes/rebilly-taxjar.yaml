description: Rebilly-based shipping amount calculation with shipping zones.
allOf:
  - $ref: ../InvoiceTax.yaml
  - type: object
    properties:
      items:
        type: array
        description: |
          Taxes applied to this invoice and calculated based on the related Product's `taxCategoryId`.
        readOnly: true
        items:
          allOf:
            - $ref: ../InvoiceTaxItem.yaml
