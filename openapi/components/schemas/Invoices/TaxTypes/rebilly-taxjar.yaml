type: object
description: TaxJar tax calculator.
required:
- calculator
properties:
  calculator:
    description: Type of tax calculator.
    type: string
  items:
    type: array
    description: Taxes that are applied to this invoice and calculated based on the related product `taxCategoryId`.
    items:
      allOf:
        - $ref: ../InvoiceTaxItem.yaml
