description: Stripe Mpis
discriminator:
  propertyName: name
  mapping:
    StripeMpi: ./StripeMpi.yaml
type: object
properties:
  name:
    allOf:
      - $ref: ../../Gateways/MpiName.yaml
    enum:
      - StripeMpi
