description: Stripe Integrated
allOf:
  - $ref: ./Stripe3dsServers.yaml
  - type: object
    properties:
      enforceThreeDSecure:
        type: boolean
        description: If true, Stripe will attempt to perform 3D Secure and overrides any [dynamic 3D Secure Radar rules](https://stripe.com/docs/radar/rules#request-3ds).
