description: Paysafe Mpis
discriminator:
  propertyName: name
  mapping:
    PaysafeMpi: ./PaysafeMpi.yaml
type: object
required:
  - name
properties:
  name:
    allOf:
      - $ref: ../../Gateways/MpiName.yaml
    enum:
      - PaysafeMpi
