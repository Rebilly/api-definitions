description: Tap Mpis
discriminator:
  propertyName: name
  mapping:
    TapMpi: ./TapMpi.yaml
type: object
required:
  - name
properties:
  name:
    allOf:
      - $ref: ../../Gateways/MpiName.yaml
    enum:
      - TapMpi
