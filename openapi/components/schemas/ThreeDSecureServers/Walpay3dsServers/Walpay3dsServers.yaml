description: Walpay 3DS Servers.
discriminator:
  propertyName: name
  mapping:
    Other: ../Other.yaml
type: object
required:
  - name
properties:
  name:
    allOf:
      - $ref: ../../Gateways/ThreeDSecureServerName.yaml
    enum:
      - Other
