type: object
properties:
  currency:
    description: |-
      Currency code in ISO 4217 format. Examples: `USD`, `EUR`, `CNY`.
    readOnly: true
    type: string
    example: USD
  invoicedAmount:
    description: Total amount of all issued, past due, or paid invoices.
    readOnly: true
    type: number
    format: double
  collectedAmount:
    description: Total amount of all paid invoices.
    readOnly: true
    type: number
    format: double
  invoiceCount:
    description: Number of issued, past due, or paid invoices.
    readOnly: true
    type: integer
