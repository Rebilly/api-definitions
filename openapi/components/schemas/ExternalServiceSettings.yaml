type: object
description: Settings for external service integration.
properties:
  quickBooksOnline:
    type: object
    description: QuickBooks Online settings.
    properties:
      syncJournalEntries:
        type: boolean
        description: Specifies if journal entries must be synchronized.
        default: false
