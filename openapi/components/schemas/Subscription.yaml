allOf:
  - $ref: ./BaseSubscription.yaml
  - type: object
    required:
      - type
      - customerId
      - websiteId
      - items
    properties:
      type:
        description: Specifies the type of order.
        type: string
        x-basic: true
        enum:
          - subscription
