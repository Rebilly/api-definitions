type: object
properties:
  data:
    type: array
    items:
      type: object
      properties:
        aggregationFieldValue:
          description: |-
            Selected aggregation field value (defined by query).
            It can be, for example, website or country.

            You define it in aggregationField param in query.
          type: string
        25th:
          description: Delay between dispute and transaction time at the 25th percentile.
          type: integer
        50th:
          description: Delay between dispute and transaction time at the 50th percentile.
          type: integer
        75th:
          description: Delay between dispute and transaction time at the 75th percentile.
          type: integer
