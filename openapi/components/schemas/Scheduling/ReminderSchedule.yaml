type: object
description: Schedule instructions collection.
properties:
  instructions:
    type: array
    items:
      $ref: ./ReminderScheduleInstruction.yaml
  chronology:
    type: string
    enum:
      - before
      - after
required:
  - instructions
  - chronology
