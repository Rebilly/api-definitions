type: object
properties:
  enableWrittenSignatureLines:
    type: boolean
    default: false
    description: Whether to enable written signature lines or not.
  organizationPrintedName:
    type: string
    description: Organization printed name. Defaults to the organization name.
  customerPrintedName:
    type: string
    description: Customer printed name. Defaults to the customer full name.
