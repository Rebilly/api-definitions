allOf:
  - $ref: ../Attachment.yaml
  - type: object
    properties:
      type:
        description: Attachment type for `website` related type. A website can have only one attachment with type `logo`.
        type: string
        default: logo
        enum:
          - logo
