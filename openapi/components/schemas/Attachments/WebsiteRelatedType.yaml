allOf:
  - $ref: ../Attachment.yaml
  - type: object
    properties:
      type:
        description: Attachment type for `website` related type.
        type: string
        default: generic
        enum:
          - logo
          - generic
        x-enumDescriptions:
          logo: A website can have only one attachment with type `logo`. Only images are valid.
          generic: No limit for generic attachments. All permitted file types are valid.
            For more information about file types, see [file details](../PostFile).
