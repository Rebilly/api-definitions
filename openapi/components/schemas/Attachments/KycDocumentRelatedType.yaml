description: Attachment for a kyc-document.
allOf:
  - $ref: ../Attachment.yaml
  - type: object
    properties:
      type:
        type: string
        enum:
          - front
          - back
          - photo
