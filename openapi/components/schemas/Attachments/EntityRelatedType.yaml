allOf:
  - $ref: ../Attachment.yaml
  - type: object
    properties:
      type:
        type: string
        description: Attachment type. Depends on `relatedType`.
        default: generic
        enum:
          - generic
