allOf:
  - $ref: ../Attachment.yaml
  - type: object
    properties:
      type:
        type: string
        description: Attachment type. Depends on `relatedType`.
          No limit for generic attachments. All permitted file types are valid.
          For more information about file types, see [file details](./PostFile).
        default: generic
        enum:
          - generic
