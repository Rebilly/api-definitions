allOf:
  - $ref: ../Attachment.yaml
  - type: object
    properties:
      type:
        description: Attachment type for `organization` related type. An organization can have only one attachment with type `logo`.
        type: string
        default: generic
        enum:
          - logo
          - generic
