type: object
properties:
  name:
    description: Exported resource.
    readOnly: true
    type: string
    enum:
      - transactions
      - customers
      - subscriptions
      - invoices
    x-enumDescriptions:
       transactions: All transactions of the merchant.
       customers: All customers of the merchant.
       subscriptions: All subscriptions of the merchant.
       invoices: All invoices of the merchant.
  recordCount:
    description: Number of exported resources.
    readOnly: true
    type: integer
