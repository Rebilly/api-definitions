description: Create TaxJar transaction to reflect the invoce.
allOf:
  - $ref: ../RuleAction.yaml
  - type: object
    properties:
      taxjarCredentialHash:
        description: Active hash of the TaxJar credential to use for performing the check.
        type: string
        example: 58816e40-387e-4834-982f-fa1855a75f64
    required:
      - taxjarCredentialHash
