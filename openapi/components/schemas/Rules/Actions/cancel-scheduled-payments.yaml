allOf:
  - $ref: ../RuleAction.yaml
  - type: object
    properties:
      cancelPolicy:
        type: string
        description: Cancel policy for scheduled invoices.
        enum:
          - all
          - exclude-started-service-period
        default: exclude-started-service-period
