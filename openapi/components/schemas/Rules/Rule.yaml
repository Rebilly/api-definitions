type: object
allOf:
  - $ref: ./Bind.yaml
  - type: object
    properties:
      final:
        description: Specifies whether the rule stops subsequent rules in the event list from being executed.
        type: boolean
        default: true
