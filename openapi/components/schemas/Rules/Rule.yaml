type: object
allOf:
  - $ref: ./Bind.yaml
  - type: object
    properties:
      final:
        description: >-
          Whether rule is final, meaning stop further matching rules if this is matched.
        type: boolean
        default: true
