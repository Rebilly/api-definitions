description: Orders resource type to export.
allOf:
  - $ref: ../DataExport.yaml
  - type: object
    properties:
      dateRange:
        type: object
        properties:
          field:
            type: string
            default: createdTime
            example: createdTime
