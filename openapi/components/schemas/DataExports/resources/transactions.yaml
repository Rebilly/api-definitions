description: Transactions resource type to export.
allOf:
  - $ref: ../DataExport.yaml
  - type: object
    properties:
      dateRange:
        type: object
        properties:
          field:
            x-sortable: true 
            type: string
            default: processedTime
            example: processedTime
