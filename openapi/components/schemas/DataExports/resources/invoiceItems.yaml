description: Invoice items resource type to export.
allOf:
  - $ref: ../DataExport.yaml
  - type: object
    properties:
      dateRange:
        type: object
        properties:
          field:
            type: string
            default: issuedTime
            example: issuedTime
