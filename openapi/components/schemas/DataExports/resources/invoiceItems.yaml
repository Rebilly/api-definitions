description: Type of invoice items resource to export.
allOf:
  - $ref: ../DataExport.yaml
  - type: object
    properties:
      dateRange:
        allOf:
          - $ref: ../DataExportDateRange.yaml
          - type: object
            properties:
              field:
                type: string
                default: issuedTime
                example: issuedTime
