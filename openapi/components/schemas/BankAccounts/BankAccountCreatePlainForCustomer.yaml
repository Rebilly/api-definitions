title: Bank Account
anyOf:
  - title: BBANType
    required:
      - customerId
    allOf:
      - properties:
          customerId:
            description: The Customer's ID.
            allOf:
              - $ref: ../ResourceId.yaml
      - $ref: ./AccountNumberTypes/BBANType.yaml
  - title: IBANType
    required:
      - customerId
    allOf:
      - properties:
          customerId:
            description: The Customer's ID.
            allOf:
              - $ref: ../ResourceId.yaml
      - $ref: ./AccountNumberTypes/IBANType.yaml

## Preference is without the discriminator even if it is more accurate.
# discriminator:
#   propertyName: accountNumberType
#   mapping:
#     BBAN: ./AccountNumberTypes/BBANType.yaml
#     IBAN: ./AccountNumberTypes/IBANType.yaml
