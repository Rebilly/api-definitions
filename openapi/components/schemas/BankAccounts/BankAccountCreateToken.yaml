type: object
required:
  - customerId
  - token
properties:
  customerId:
    $ref: ../CustomerId.yaml
  token:
    description: BankAccountToken ID.
    type: string
  customFields:
    $ref: ../ResourceCustomFields.yaml
  useAsBackup:
    $ref: ../UseAsBackup.yaml
