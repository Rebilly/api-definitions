type: object
required:
  - customerId
  - token
properties:
  customerId:
    description: The Customer's ID.
    allOf:
      - $ref: ../ResourceId.yaml
  token:
    description: BankAccountToken ID.
    type: string
  customFields:
    $ref: ../ResourceCustomFields.yaml
