description: Alternative payment method instrument.
title: Alternative instrument
type: object
required:
  - method
properties:
  method:
    $ref: ../../PaymentMethods/PaymentMethod.yaml
  paymentInstrumentId:
    description: |
      The payment instrument ID. It will be empty if there is no billing address within the transaction or customer.
