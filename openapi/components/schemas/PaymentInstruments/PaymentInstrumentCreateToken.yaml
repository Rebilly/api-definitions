title: Payment token
type: object
required:
  - customerId
  - token
properties:
  customerId:
    $ref: ../CustomerId.yaml
  token:
    description: Token ID of the payment.
    type: string
  customFields:
    $ref: ../ResourceCustomFields.yaml
  useAsBackup:
    $ref: ../UseAsBackup.yaml
