description: Cash payment instrument object
allOf:
  - $ref: ../PaymentInstrument.yaml
  - type: object
    properties:
      receivedBy:
        description: The receiver's name
        type: string
