type:
  - 'object'
  - 'null'
description: Setup fee information for the plan.
required:
  - price
properties:
  price:
    description: |-
      One-time setup fee charged when the plan starts. Set to `0` if none applies.
    type: number
    format: double
