description: DataCash Mpis
discriminator:
  propertyName: name
  mapping:
    DataCashMpi: ./DataCashMpi.yaml
type: object
required:
  - name
properties:
  name:
    allOf:
      - $ref: ../../Gateways/MpiName.yaml
    enum:
      - DataCashMpi
