description: SecureTrading Mpis
discriminator:
  propertyName: name
  mapping:
    SecureTradingMpi: ./SecureTradingMpi.yaml
type: object
required:
  - name
properties:
  name:
    allOf:
      - $ref: ../../Gateways/MpiName.yaml
    enum:
      - SecureTradingMpi
