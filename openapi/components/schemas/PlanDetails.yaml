type: object
properties:
  type:
    type: string
    enum:
      - subscription
      - one-time-sale
      - trial-only
discriminator:
  propertyName: type
  mapping:
    subscription: ./SubscriptionPlanDetails.yaml
    one-time-sale: ./OneTimeSalePlanDetails.yaml
    trial-only: ./TrialOnlyPlanDetails.yaml
oneOf:
  - $ref: ./SubscriptionPlanDetails.yaml
  - $ref: ./OneTimeSalePlanDetails.yaml
  - $ref: ./TrialOnlyPlanDetails.yaml
