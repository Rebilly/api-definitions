type: object
title: Unrestricted
description: Currencies metadata.
required:
  - mode
properties:
  mode:
    type: string
    description: Specifies how the currencies are compared.
    enum:
      - unknown
      - all
