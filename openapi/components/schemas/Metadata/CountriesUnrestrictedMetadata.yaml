type: object
title: Unrestricted
required:
  - mode
  - values
properties:
  mode:
    type: string
    enum:
      - unknown
      - all
    values:
      description: List of supported countries.
      type: array
