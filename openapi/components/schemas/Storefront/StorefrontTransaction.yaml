allOf:
  - $ref: ../Common/CommonTransaction.yaml
  - properties:
      approvalUrl:
        description: The URL to redirect the end-customer when transaction `status` is `waiting-approval`.
        type: string
        format: uri
