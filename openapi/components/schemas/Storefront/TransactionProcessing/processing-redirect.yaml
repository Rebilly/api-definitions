type: object
title: Redirect
properties:
  type:
    enum:
      - redirect
  url:
    type: string
    description: The URL customer form should be sent to.
    readOnly: true
    format: url
  params:
    type: object
    description: The extra query params.
    readOnly: true
    nullable: true
