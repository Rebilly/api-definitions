type: object
title: Query
properties:
  type:
    enum:
      - query
  interval:
    type: integer
    description: The interval transaction processing state should be updated at.
    readOnly: true
