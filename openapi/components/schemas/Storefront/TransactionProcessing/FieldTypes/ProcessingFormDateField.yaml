type: object
title: Date
properties:
  type:
    enum:
      - date
  containsDay:
    type: boolean
    description: Should the date contain a day of the month.
  containsMonth:
    type: boolean
    description: Should the date contain a month.
  contiainsYear:
    type: boolean
    description: Should the date contain a year.
