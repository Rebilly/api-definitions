type: object
properties:
  url:
    type: string
    format: uri
    description: URL from which the score is obtained.
  details:
    type: string
    description: Text from which the score is obtained.
