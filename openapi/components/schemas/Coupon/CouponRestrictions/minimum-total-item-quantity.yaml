description: minimum-total-item-quantity restrictions.
allOf:
  - $ref: ../CouponRestriction.yaml
  - $ref: ../RedemptionRestriction.yaml
  - type: object
    required:
      - quantity
    properties:
      quantity:
        type: integer
        description: Minimum Invoice quantity total.
