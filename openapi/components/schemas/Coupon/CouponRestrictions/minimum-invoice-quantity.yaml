description: Restricts a coupon redemption to invoice items with the quantity greater or equal to specified.
allOf:
  - $ref: ../CouponRestriction.yaml
  - $ref: ../RedemptionRestriction.yaml
  - type: object
    required:
      - quantity
      - productIds
    properties:
      quantity:
        type: integer
        description: Minimum Invoice quantity.
      productIds:
        type: array
        description: Product IDs on which to filter the restriction and subsequent discount. If empty, then all items.
        items:
          type: string
