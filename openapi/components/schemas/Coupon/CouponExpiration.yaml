type: object
required:
  - expiredTime
properties:
  expiredTime:
    description: >-
      The coupon's expiry time, must be greater than the issued time. Null or
      empty string will immediately expire the coupon.
    type: string
    format: date-time
