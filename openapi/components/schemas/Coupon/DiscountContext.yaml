type: string
description: >-
  The context in which the discount applies.
  `items`: `items - discount`.
  `shipping`: `shipping - discount`.
  `items-and-shipping`: `items + shipping - discount`.
default: items
x-label: Context
x-sortable: true
x-basic: true
x-enumDescriptions:
  items: items (less discounts)
  shipping: shipping (less discounts)
  items-and-shipping: items and shipping (less discounts)
enum:
  - items
  - shipping
  - items-and-shipping
