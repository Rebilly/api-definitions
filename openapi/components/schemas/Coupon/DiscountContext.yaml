type: string
description: >-
  The context in which the discount applies.
  `items`: `items - discount`
  `shipping`: `shipping - discount`
  `subtotal`: `items + shipping - discount`
default: subtotal
x-label: Context
x-sortable: true
x-basic: true
enum:
  - items
  - shipping
  - subtotal
