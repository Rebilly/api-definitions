type: object
allOf:
  - $ref: ./CommonSubscription.yaml
  - properties:
      status:
        description: One-time order status.
        type: string
        readOnly: true
        enum:
          - pending
          - completed
          - abandoned
