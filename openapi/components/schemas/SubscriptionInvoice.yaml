type: object
properties:
  transactionId:
    description: >-
      If present, applies a payment to the invoice created.  If the payment is
      for the invoice total, it would be marked as paid.
    allOf:
      - $ref: ./ResourceId.yaml
