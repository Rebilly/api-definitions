type: object
allOf:
  - $ref: ./RulesBind.yaml
  - type: object
    properties:
      final:
        description: Specifies if the rule stops subsequent rules in the event list from being executed.
        type: boolean
        default: true
