description: SMSVoucher Gateway config.
allOf:
  - $ref: ../GatewayAccount.yaml
  - type: object
    required:
      - credentials
    properties:
      credentials:
        type: object
        properties:
          AppId:
            type: string
            description: SMSVoucher AppId.
        required:
          - AppId
