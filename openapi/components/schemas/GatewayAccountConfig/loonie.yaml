description: loonie config.
type: object
required:
  - credentials
properties:
  credentials:
    type: object
    description: loonie credentials object.
    properties:
      merchantId:
        type: string
      merchantToken:
        type: string
        format: password
    required:
      - merchantId
      - merchantToken
