description: Cashterminal config.
allOf:
  - $ref: ../GatewayAccount.yaml
  - type: object
    required:
      - credentials
    properties:
      credentials:
        type: object
        properties:
          secretKey:
            type: string
            format: password
        required:
          - secretKey
