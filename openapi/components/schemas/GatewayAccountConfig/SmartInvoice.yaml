description: SmartInvoice config
allOf:
  - $ref: ../GatewayAccount.yaml
  - type: object
    required:
      - credentials
    properties:
      credentials:
        type: object
        description: SmartInvoice credentials object
        properties:
          merchantId:
            type: string
            description: SmartInvoice merchant account UID
          applicationId:
            type: string
            description: SmartInvoice application UID
            format: password
        required:
          - merchantId
          - applicationId
      mpi:
        deprecated: true
        allOf:
          - $ref: ../ThreeDSecureServers/SmartInvoice3dsServers/SmartInvoice3dsServers.yaml
      threeDSecureServer:
        $ref: ../ThreeDSecureServers/SmartInvoice3dsServers/SmartInvoice3dsServers.yaml
