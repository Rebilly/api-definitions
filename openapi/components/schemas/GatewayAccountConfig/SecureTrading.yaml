description: SecureTrading gateway configuration.
allOf:
  - $ref: ../GatewayAccount.yaml
  - type: object
    required:
      - credentials
    properties:
      credentials:
        type: object
        description: SecureTrading credentials object.
        properties:
          websiteId:
            type: string
            description: SecureTrading website ID.
            maxLength: 50
            example: web_01GQMKH86SKH34SR7TN68MFYRD
          username:
            type: string
            description: SecureTrading web service username.
          password:
            type: string
            description: SecureTrading web service password.
            format: password
            writeOnly: true
        required:
          - websiteId
          - username
          - password
      threeDSecureServer:
        $ref: ../ThreeDSecureServers/SecureTrading3dsServers/SecureTrading3dsServers.yaml
