description: BankSEND config.
allOf:
  - $ref: ../GatewayAccount.yaml
  - type: object
    required:
      - credentials
    properties:
      credentials:
        type: object
        description: BankSEND credentials object.
        properties:
          merchantId:
            type: string
          merchantToken:
            type: string
            format: password
        required:
          - merchantId
          - merchantToken
