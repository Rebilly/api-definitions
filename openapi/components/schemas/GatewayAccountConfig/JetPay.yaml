description: JetPay Gateway config.
allOf:
  - $ref: ../GatewayAccount.yaml
  - type: object
    required:
      - credentials
    properties:
      credentials:
        type: object
        properties:
          TerminalID:
            type: string
            description: JetPay Gateway terminal ID.
        required:
          - TerminalID
