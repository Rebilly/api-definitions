description: Aircash gateway configuration.
allOf:
  - $ref: ../GatewayAccount.yaml
  - type: object
    required:
      - credentials
    properties:
      credentials:
        type: object
        description: Aircash credentials object.
        properties:
          privateKey:
            writeOnly: true
            type: string
            description: Private key of the Aircash partner.
          privateKeyPass:
            writeOnly: true
            type: string
            description: Private key passphrase of the Aircash partner.
            format: password
          aircashPublicKey:
            type: string
            description: Public key of the Aircash partner.
          partnerId:
            type: string
            description: Partner id.
        required:
          - privateKey
          - privateKeyPass
          - aircashPublicKey
          - partnerId
