description: Conekta config.
type: object
required:
  - credentials
properties:
  credentials:
    type: object
    description: Conekta credentials object.
    properties:
      apiKey:
        type: string
        description: Conekta private API key.
        format: password
    required:
      - apiKey
