description: PayPal Gateway config
allOf:
  - $ref: ../GatewayAccount.yaml
  - type: object
    required:
      - credentials
    properties:
      credentials:
        type: object
        properties:
          merchantIdInPayPal:
            type: string
            description: PayPal Gateway merchant id
        required:
          - merchantIdInPayPal
