description: Circle config.
type: object
required:
  - credentials
properties:
  credentials:
    type: object
    description: Circle credentials object.
    properties:
      apiKey:
        type: string
        format: password
    required:
      - apiKey
