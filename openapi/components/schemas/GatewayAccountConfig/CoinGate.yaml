description: CoinGate Gateway config.
allOf:
  - $ref: ../GatewayAccount.yaml
  - type: object
    required:
      - credentials
    properties:
      credentials:
        type: object
        properties:
          authToken:
            type: string
            format: password
        required:
          - authToken
