allOf:
  - $ref: ./TimelineTable.yaml
  - type: object
    properties:
      data:
        type: array
        description: Table data. Each array element represents a line.
        items:
          type: object
          properties:
            summary: 
              type: string
            diff:
              type: array
              description: Diff changes. Each array element represents a change summary.
            items:
              type: string
