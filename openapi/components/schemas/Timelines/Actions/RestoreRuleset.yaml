type: object
description: Restore ruleset version.
readOnly: true
required:
  - action
properties:
  version:
    type: integer
    description: Version of the ruleset to restore.
  action:
    type: string
    enum:
      - ruleset_restore
