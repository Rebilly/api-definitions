type: object
description: Resend email action.
readOnly: true
required:
  - action
properties:
  messageId:
    allOf:
      - $ref: ../../ResourceId.yaml
  action:
    type: string
    enum:
      - resend-email
