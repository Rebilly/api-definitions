type: object
description: Cancel redemption.
readOnly: true
required:
  - action
properties:
  redemptionId:
    allOf:
      - $ref: ../../ResourceId.yaml
  action:
    type: string
    enum:
      - cancel_redemption
