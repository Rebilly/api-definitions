type: object
description: Show Enhanced Due Diligence (EDD) search logs action.
required:
    - action
readOnly: true
properties:
    searchLogId:
        allOf:
            - $ref: ../../ResourceId.yaml
    type:
        type: string
        enum:
            - arrest
            - bankruptcy
            - fraud
            - occupation
    action:
        type: string
        enum:
            - show-edd-search-logs
