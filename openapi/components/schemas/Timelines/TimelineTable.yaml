type: object
description: Additional timeline message information.
discriminator:
  propertyName: type
  mapping:
    three-columns: ./ThreeColumnsTimeTable.yaml
    two-columns: ./TwoColumnsTimeTable.yaml
    one-column: ./OneColumnTimelineTable.yaml
    list: ./ListTimeTable.yaml
properties:
  type:
    type: string
    enum:
      - list
      - one-columns
      - two-columns
      - three-columns
  title:
    type: string
    description: Data table title.
  footer:
    type: string
    description: Data table footer.
