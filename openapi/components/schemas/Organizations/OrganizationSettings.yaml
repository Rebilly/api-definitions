type: object
description: Organization settings.
properties:
  defaultTaxCalculator:
    type: object
    description: Tax calculation settings.
    required:
      - type
    properties:
      type:
        type: string
        description: Type of tax calculator.
        enum:
          - taxjar
          - flat
      rate:
        type: number
        nullable: true
        format: float
        description: Rate for flat tax calculation.
  billing:
    description: Global organization settings for billing.
    type: object
    nullable: true
    properties:
      pendingOrderTtl:
        description: TTL in seconds before pending order is automatically voided.
        type: integer
        nullable: true
        default: null
        minimum: 8640
        maximum: 31536000
  taxLocations:
    type: array
    description: |-
      Additional organization addresses where a merchant may want to collect taxes using the `taxjar` tax calculator.

      Available only for US-based organizations.
    items:
      $ref: ./TaxLocation.yaml
