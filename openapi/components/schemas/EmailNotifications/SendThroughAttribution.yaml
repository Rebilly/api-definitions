type: object
description: Email notification sent through attribution.
readOnly: true
properties:
  eventType:
    $ref: ../EventType.yaml
  actionId:
    type: string
    format: uuid
    description: Action ID of the email notification.
  versionId:
    type: string
    format: uuid
    description: Version ID of the email notification.
  sent:
    type: integer
    minimum: 0
    description: Number of email notifications that have been sent.
  goal:
    type: integer
    minimum: 0
    description: Ideal, or target, number of email notifications to send.
