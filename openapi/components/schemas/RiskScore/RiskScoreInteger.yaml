type: object
nullable: true
required:
  - scoreValue
  - threshold
properties:
  scoreValue:
    type: integer
    description: This value is added to transaction risk score.
  threshold:
    type: integer
    description: Condition that has to be reached by a risk factor to increase transaction risk score.
