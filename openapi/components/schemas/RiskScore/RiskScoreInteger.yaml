type: object
nullable: true
required:
  - scoreValue
  - threshold
properties:
  scoreValue:
    type: integer
    description: Value added to the risk score of the transaction.
  threshold:
    type: integer
    description: |-
      Threshold of the risk score.
      If this value is exceeded by a risk factor, the risk score of the related transaction is increased.
