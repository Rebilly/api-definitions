type: object
properties:
  riskScoreThreshold:
    type: integer
  ttl:
    type: integer
    description: |-
      Length of time a blocklist exists before it expires.
      The default field value for this field is `0`, which means that the blocklist record never expires.
    default: 0
