type: object
nullable: true
required:
  - riskScoreThreshold
  - ttl
properties:
  riskScoreThreshold:
    type: integer
  ttl:
    type: integer
    description: |-
      Length of time a blocklist exists before it expires.
      If value for this field is `0` then blocklist record never expires.
