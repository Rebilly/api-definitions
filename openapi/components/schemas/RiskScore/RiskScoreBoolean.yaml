type: object
nullable: true
properties:
  scoreValue:
    type: integer
    description: This value will be added to transaction's risk score.
  threshold:
    type: integer
    description: Condition that has to be reached by a risk factor to increase transaction's risk score.
