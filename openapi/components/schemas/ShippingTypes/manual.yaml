description: Manually set shipping amount.
allOf:
  - $ref: ../Shipping.yaml
  - type: object
    required:
      - amount
    properties:
      amount:
        x-type: Money
        type: integer
        description: The invoice's shipping amount.
