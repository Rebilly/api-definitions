description: Rebilly-based shipping which is calculated by shipping rates.
allOf:
  - $ref: ../Shipping.yaml
  - type: object
    properties:
      rateId:
        description: The shipping rate ID. If unset the cheapest applicable shipping rate is chosen.
        nullable: true
        allOf:
          - $ref: ../ResourceId.yaml
      amount:
        type: integer
        readOnly: true
        description: The invoice's shipping amount which is calculated from [Shipping rates](#tag/Shipping-rates).
