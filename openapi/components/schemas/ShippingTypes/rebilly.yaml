description: Rebilly-based shipping which is calculated by shipping zones.
allOf:
  - $ref: ../Shipping.yaml
  - type: object
    properties:
      shippingRateId:
        description: The shipping rate ID. If unset – cheapest shipping rate among applicable would be chosen.
        nullable: true
        allOf:
          - $ref: ../../ResourceId.yaml
      amount:
        type: integer
        readOnly: true
        description: The invoice's shipping amount which is calculated from [Shipping Zones](#tag/Shipping-Zones).
