description: Void an Intuit QuickBooks invoice.
allOf:
  - $ref: ./RuleAction.yaml
  - type: object
    properties:
      credentialHash:
        type: string
        description: ID of the OAuth2 credential.
    required:
      - credentialHash
