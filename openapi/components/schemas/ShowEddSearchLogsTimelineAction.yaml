type: object
description: Show Enhanced Due Diligence (EDD) search logs action.
readOnly: true
properties:
  action:
    type: string
    enum:
      - show-edd-search-logs
  searchLogId:
    $ref: ./ResourceId.yaml
  type:
    type: string
    enum:
      - arrest
      - bankruptcy
      - fraud
      - occupation
