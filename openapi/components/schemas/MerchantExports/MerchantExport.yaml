type: object
properties:
  id:
    readOnly: true
    allOf:
      - $ref: ../ResourceId.yaml
  userId:
    description: The ID of the User who requested the merchant data export.
    readOnly: true
    type: string
  status:
    description: Status of merchant data export request.
    readOnly: true
    type: string
    enum:
      - pending
      - processing
      - completed
      - queued
      - failed
  items:
    description: Merchant data export resources array.
    readOnly: true
    type: array
    items:
      $ref: ../MerchantExports/MerchantExportResource.yaml
  createdTime:
    description: The time the merchant data export was requested.
    readOnly: true
    allOf:
      - $ref: ../ServerTimestamp.yaml
  updatedTime:
    description: Merchant data export updated time.
    readOnly: true
    allOf:
      - $ref: ../ServerTimestamp.yaml
  retentionTime:
    description: Retention date when the files will be removed.
    type: string
    format: date-time
  _links:
    type: array
    description: The links related to resource.
    readOnly: true
    minItems: 1
    maxItems: 3
    items:
      anyOf:
        - $ref: ../Links/LinkSelf.yaml
        - $ref: ../Links/LinkUser.yaml
        - $ref: ../Links/LinkSignedLink.yaml

