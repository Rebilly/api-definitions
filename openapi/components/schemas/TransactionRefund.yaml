type: object
required:
  - amount
properties:
  amount:
    description: Refund amount.
    type: number
    format: double
  description:
    description: Refund description.
    type: string
    maxLength: 255
  isProcessedOutside:
    description: True if refund is processed outside Rebilly.
    type: boolean
    default: false
