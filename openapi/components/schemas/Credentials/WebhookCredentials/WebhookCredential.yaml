allOf:
  - $ref: ../Credential.yaml
  - type: object
    description: Webhook credential
    properties:
      host:
        type: string
        description: The host name
      auth:
        $ref: ./WebhookAuthorization.yaml
    required:
      - host
