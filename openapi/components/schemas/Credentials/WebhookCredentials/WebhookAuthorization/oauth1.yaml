allOf:
  - $ref: ../WebhookAuthorization.yaml
  - type: object
    properties:
      consumerKey:
        type: string
      consumerSecret:
        type: string
      token:
        type: string
      tokenSecret:
        type: string
    required:
      - consumerKey
      - consumerSecret
      - token
      - tokenSecret
