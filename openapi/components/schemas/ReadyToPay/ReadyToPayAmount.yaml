type: object
title: 'With amount'
required:
  - currency
  - amount
properties:
  currency:
    allOf:
      - $ref: ../CurrencyCode.yaml
  amount:
    description: The amount
    type: number
    format: double
