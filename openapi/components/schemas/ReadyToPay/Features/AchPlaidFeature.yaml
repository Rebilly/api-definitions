type: object
title: Plaid
properties:
  name:
    type: string
    description: Feature name.
    enum:
      - Plaid
  linkToken:
    type: string
    description: Plaid `linkToken` for frontend integrations.
  expirationTime:
    type: string
    format: date-time
    description: Expiration time of a `linkToken`.
