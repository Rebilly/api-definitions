allOf:
  - title: Bad request
  - type: object
    properties:
      status:
        type: integer
        description: HTTP status code.
        minimum: 400
        maximum: 400
      invalidFields:
        $ref: ./InvalidFields.yaml
  - $ref: ./BaseProblem.yaml
