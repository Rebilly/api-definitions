type: object
properties:
  username:
    description: Authentication username.
    type: string
  password:
    description: Authentication password.
    type: string
    format: password
  type:
    type: string
    enum: [ "login" ]
    default: none
required:
  - username
  - password
  - type
