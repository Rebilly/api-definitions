type: object
properties:
  name:
    type: string
    maxLength: 255
    pattern: '^[@~\-\.\w\s]+$'
    example: New
    description: |-
      Unique name for the tag.
      Tag names are not case-sensitive.
