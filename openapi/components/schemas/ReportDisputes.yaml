type: object
properties:
  data:
    type: array
    items:
      type: object
      properties:
        aggregationValue:
          description: >
            Selected aggregation field value (defined by query). It can be, for
            example, website or country.

            You define it in aggregationField param in query.
          type: string
        countVisa:
          description: Disputes count on Visa.
          type: integer
        ratioCountVisa:
          description: Disputes count / Settled transactions count.
          type: number
          format: double
        ratioAmountVisa:
          description: Disputes amount / Settled transactions amount.
          type: number
          format: double
        countMastercard:
          description: Disputes count on Mastercard.
          type: integer
        ratioCountMastercard:
          description: Disputes count / Settled transactions count.
          type: number
          format: double
        ratioAmountMastercard:
          description: Disputes amount / Settled transactions amount.
          type: number
          format: double
