name: resource
in: path
description: Type of resource.
required: true
schema:
  type: string
  enum:
    - customers
    - invoices
    - invoice-items
    - transactions
    - journal-accounts
    - journal-entries
