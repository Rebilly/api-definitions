name: filter
in: query
description: |-
  Filters the collection items. This field requires
  a special format. Use `,` for multiple allowed values.  Use `;` for multiple fields.

  For more information, see [Filter data](https://www.rebilly.com/docs/dev-docs/query-and-expand/#filter-data).
schema:
  type: string
