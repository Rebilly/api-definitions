name: resource
in: path
description: Resource type string.
required: true
schema:
  type: string
  enum:
    - customers
    - invoices
    - invoice-items
    - transactions
    - journal-accounts
    - journal-entries
