name: Organization-Id
in: header
description: >-
  Organization identifier in scope of which need to perform request (if not
  specified, the default organization will be used).


  It is deprecated. Use servers with `/organizations/{organizationId}` base path instead.
deprecated: true
schema:
  $ref: ../schemas/ResourceId.yaml
required: false
