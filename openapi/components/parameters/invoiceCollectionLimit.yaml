name: limit
in: query
description: |-
  Limit the number of invoices to return.
  Use `0` to return an empty collection and still receive the count of matching items in `Pagination-Total`.
  Cap values greater than `100` at `100`.
schema:
  type: integer
  minimum: 0
  maximum: 100
