name: version
in: path
required: true
description: |-
  Version of the ruleset.

  To retrieve the full related object instead of the ID, expand the response.
  For more information, see [Expand to include embedded objects](https://www.rebilly.com/docs/dev-docs/concepts/#expand-requests-to-include-embedded-objects).
schema:
  type: integer
  minimum: 1
