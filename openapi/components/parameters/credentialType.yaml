name: resource
in: path
description: Credential hash type.
required: true
schema:
  type: string
  enum:
    - smtp
    - webhook
    - mailgun
    - send-grid
    - aws-ses
    - oauth2
    - postmark
    - experian
    - taxjar
    - avalara
    - plaid
