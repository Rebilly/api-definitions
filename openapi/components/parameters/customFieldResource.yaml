name: resource
in: path
description: Resource type string.
required: true
schema:
  type: string
  enum:
    - customers
    - payment-instruments
    - subscriptions
    - transactions
    - websites
    - products
    - plans
    - bump-offers
