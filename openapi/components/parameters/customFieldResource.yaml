name: resource
in: path
description: Type of resource schema.
required: true
schema:
  type: string
  enum:
    - customers
    - payment-instruments
    - orders
    - subscriptions
    - transactions
    - websites
    - products
    - plans
    - gateway-accounts
    - deposit-requests
