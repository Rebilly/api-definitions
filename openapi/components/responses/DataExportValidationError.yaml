description: Invalid data sent, or the export exceeds the maximum record limit.
content:
  application/json:
    schema:
      oneOf:
        - $ref: ../schemas/ProblemTypes/ValidationError.yaml
        - $ref: ../schemas/ProblemTypes/DataExportLimitExceeded.yaml
